Project to retrieve information from the official Sitges Film Festival website, transform and format it to be useful

The source website, output file and banned sections can be customized using the settings.json file

To get the list of movies just run "python start.py". An output html file will be saved as per settings.json